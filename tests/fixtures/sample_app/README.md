# sample_app

A tiny demo project for the NexCoder integration tests.

```python
from sample_app.src.math_utils import add
assert add(1, 2) == 3
```

Run the tests with:

```bash
python -m pytest tests/
```
