"""Sample Python project used by integration tests.

A small but realistic layout so the agent loop, context builder, and
tool registry all have something to chew on:

- A package with a real module that contains a function
- A test file that exercises the function
- A README describing the project
- A pyproject.toml declaring the package metadata
"""

# This file intentionally left blank — a real package marker.
